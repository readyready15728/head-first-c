Example #1:

* Will compile
* Will work

Example #2:

* Will compile
* Will emit a warning
* Will work

Example #3:

* Won't even compile

Example #4:

* Will compile but with `float` implicitly cast to `int`.
